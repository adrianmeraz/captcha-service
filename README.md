## 2Captcha

## Troubleshooting

Boto3 Stubber cannot mock Table Resource correctly.

For example, "put_item" will give an error that fields need to be serialized.

Stubber can be used for boto client mocks, but not resource.